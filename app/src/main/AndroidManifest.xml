<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mrosarioinc.proyectofinal">

    <application
        android:name=".CustomAppClass"
        android:allowBackup="true"
        android:icon="@drawable/mexicanito"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SisarSalon"
            android:label="@string/title_activity_sisar_salon"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SisarControl" />
        <activity
            android:name=".Menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Opciones" />
        <activity
            android:name=".CrearUsuario"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>